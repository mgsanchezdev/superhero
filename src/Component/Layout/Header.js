import React from 'react'
import {Button} from 'react-bootstrap';

import './Header.css';

const Header = () => (
    <div className = "container">
    <Button variant="success" >Buscar heroe</Button>
    </div>
    )

export default Header
